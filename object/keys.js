// Object.keys() O método estático retorna uma matriz de nomes de propriedades enumeráveis ​​com chave de string do próprio objeto.

const object1 = {
    a: 'somestring',
    b: 42,
    c: false,
  };
  
  console.log(Object.keys(object1)); // Expected output: Array ["a", "b", "c"]
  